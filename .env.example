# Docker Compose Build Arguments
# These are used during the frontend build process

# API URL - Backend API endpoint
VITE_API_URL=https://api.certs.gdg-oncampus.dev

# Admin Hostname - Admin interface domain
VITE_ADMIN_HOSTNAME=sudo.certs-admin.certs.gdg-oncampus.dev

# Public Hostname - Public validation page domain
VITE_PUBLIC_HOSTNAME=certs.gdg-oncampus.dev

# PostgreSQL Database Configuration
# IMPORTANT: Change these values in production for security
POSTGRES_DB=gdgoc_certs
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_secure_password_in_production

# Database connection for backend
# These should match the PostgreSQL values above
DB_HOST=db
DB_PORT=5432
DB_NAME=gdgoc_certs
DB_USER=postgres
DB_PASSWORD=change_this_secure_password_in_production

# SMTP Configuration
# Configure your SMTP provider settings below
# Common providers: Brevo (smtp-relay.brevo.com), Gmail (smtp.gmail.com), SendGrid, etc.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=starttls
# SMTP_SECURE options: 
#   - starttls: Use STARTTLS (port 587, recommended)
#   - ssl: Use SSL/TLS (port 465)
#   - none: No encryption (port 25, not recommended)
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM="GDGoC Certificates" <noreply@gdg-oncampus.dev>

# Public Hostname (used in email links)
PUBLIC_HOSTNAME=certs.gdg-oncampus.dev

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=https://sudo.certs-admin.certs.gdg-oncampus.dev,https://certs.gdg-oncampus.dev
